# salon-management-system

Frontend of a web application to manage a beauty salon (including bill, income, staff, service, voucher managements)

### In this project, JSON object is preferred to CLASS object because it is easy to handle dynamic inputs with JSON object. We can dynamically and efficiently assign and validate inputs using the property names of each object.
